import React from 'react'

interface AuthModalProps {
    sessionID: number
}

const AuthModal: React.FC<AuthModalProps> = ({sessionID: number}) => {
  return (
    <div className="">
        if(sessionID)
    </div>
  )
}

export default AuthModal